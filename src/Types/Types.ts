
export type ContextProps = {
    children: React.ReactNode
};
export type PropsAction = {
    type: string,
    payload: number
}
export type trasectiommap = {
    text: string;
    amount: number
    id: number
}
